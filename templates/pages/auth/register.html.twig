{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Créer mon compte</button>
    {{ form_end(form) }}
{% endblock %}